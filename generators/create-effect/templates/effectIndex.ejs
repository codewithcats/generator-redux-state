// Auto-generated by the generator
// Don't manually edit this file

// @flow

<% for (fx in effects) {
  const {name} = effects[fx]
-%>
import {<%= name %>Effect} from './<%= name %>'
<% } -%>

export const <%= state %>Effects = [
<% for (fx in effects) {
  const {name} = effects[fx]
-%>
  <%= name %>Effect,
<% } -%>
]
