// Auto-generated by the generator
// Don't manually edit this file

// @flow

<% for (ch in channels) {
  const {name} = channels[ch]
-%>
import {<%= name %>} from './<%= name %>'
<% } -%>

export const <%= state %>Channels = {
<% for (ch in channels) {
  const {name} = channels[ch]
-%>
  <%= name %>,
<% } -%>
}
