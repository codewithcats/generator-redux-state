// Auto-generated by the generator
// Don't manually edit this file

// @flow

import type {Action} from '../../common.type'
import type {<%= stateType %>} from '../type'

<% actions.forEach(action => { -%>
import {<%= action.name %>} from '../actions/<%= action.name %>'
<% }) -%>

<% actions.forEach(action => { -%>
import {<%= action.name %>Reducer} from './<%= action.name %>'
<% }) -%>

import {initialState} from './initialState'

export const <%= stateName %>Reducer = (state: <%= stateType %> = initialState, action: Action<any>) => {
  switch (action.type) {
<% actions.forEach(action => { -%>
    case <%= action.name %>.type:
      return <%= action.name %>Reducer(state, action)
<% }) -%>
    default:
      return state
  }
}
